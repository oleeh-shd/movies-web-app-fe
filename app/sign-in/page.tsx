import { SignInForm } from "@/components/sign-in-form";

const SignIn = () => {
    return (
        <section className="my-auto flex size-full items-center justify-center">
            <SignInForm />
        </section>
    );
};

export default SignIn;
